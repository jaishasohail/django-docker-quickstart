---
# IMPORTANT: Encrypt this file using ansible-vault
# ansible-vault encrypt vars/secrets.yml

# Django secrets
django_secret_key: "your-super-secret-key-here-change-in-production"
django_admin_password: "change-me-in-production"

# Database credentials (alternative to AWS Secrets Manager)
db_password: "your-database-password"

# Redis credentials (if AUTH is enabled)
redis_password: ""

# AWS credentials (if not using IAM roles)
aws_access_key_id: ""
aws_secret_access_key: ""

# Docker registry credentials
docker_username: "AWS"
docker_password: "" # ECR token, generated dynamically

# SSL/TLS certificates (if not using cert-manager)
tls_certificate: ""
tls_private_key: ""
# Additional secrets
# Add any other sensitive configuration here
